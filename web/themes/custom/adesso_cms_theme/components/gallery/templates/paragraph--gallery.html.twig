{% set is_dark = paragraph.field_content_element_theme.value %}
{%
  set classes = [
    'container',
    'my-6 my-lg-15',
    'py-8',
    'lg:py-16',
    'mx-auto',
    'max-w-screen-xl',
    'px-4',
     is_dark ? 'bg-gray-800 text-white' : 'bg-white text-gray-900' ,
  ]
%}
<div{{ attributes.addClass(classes) }}>
  {{ title_prefix }}
  {{ title_suffix }}
  {% block content %}
    {% embed 'adesso_cms_theme:gallery' with {
        section_title: content.field_title|render,
        pre_headline: content.field_pre_headline|render,
        sub_headline: content.field_sub_headline|render,
        content: content,
        is_dark: is_dark,
      } only %}
      {% block gallery_items %}
        {% for i in content.field_media_item['#items'] %}
          {{ content.field_media_item[loop.index0] }}
        {% endfor %}
      {% endblock %}
    {% endembed %}
  {% endblock %}
</div>
