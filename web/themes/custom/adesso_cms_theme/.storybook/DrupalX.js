import { create } from '@storybook/theming/create';

export default create({
  base: 'light',
  brandTitle: 'adesso CMS',
  brandUrl: '/',
  brandImage: './images/logo.svg',
  brandTarget: '_self',
});
