{#
/**
 * @file
 * Pricing paragraph template using paragraph-wrapper component with embed pattern.
 */
#}

{# Pricing paragraph configuration #}
{% include '@adesso_cms_theme/_mixins/paragraph-config.twig' %}
{# Embed the paragraph-wrapper component using slots #} {% embed 'adesso_cms_theme:paragraph-wrapper' with { theme:
wrapper_theme, spacing: wrapper_spacing, tag: wrapper_tag, className: wrapper_css_class, attributes: attributes } %} {%
block prefix %} {{ title_prefix }} {{ title_suffix }} {% endblock %} {% block content %} {% embed
'adesso_cms_theme:pricing' with { content: content, pre_headline: paragraph.field_eyebrow.value, title:
paragraph.field_title.value, summary: content.field_summary, grid_columns: content.field_pricing_cards['#items']|length
> 0 ? (content.field_pricing_cards['#items']|length > 3 ? '3' : content.field_pricing_cards['#items']|length ~ '') :
'3', is_dark: wrapper_theme == 'dark', } only %} {% block pricing_cards %} {{ content.field_pricing_cards }} {% endblock
pricing_cards %} {% endembed %} {% endblock %} {% endembed %}
