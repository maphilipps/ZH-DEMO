{% set classes = [
    'headline',
    'text-3xl lg:text-7xl font-semibold tracking-tight',
     color ?? 'text-black'
] %}

<{{ as ?? 'h2' }}{{ attributes.addClass(classes) }}>
{{ text }}
</{{ as ?? 'h2' }}>