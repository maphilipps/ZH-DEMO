{# /** 
 * @file 
 * Stats item paragraph template (child component - no wrapper needed). 
 */ #}

{{ title_prefix }}
{{ title_suffix }}

{% embed 'adesso_cms_theme:stat-card' with { 
  media: content.field_custom_icon|field_value ? content.field_custom_icon : '', 
  body: content.field_summary, 
  icon: content.field_icon|field_value, 
  layout: 'center', 
  border: false, 
} only %}
  {% block heading %}{{ content.field_title }}{% endblock %}
{% endembed %}
