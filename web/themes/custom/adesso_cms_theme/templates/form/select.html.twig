{#
/**
 * @file
 * Theme override for select elements.
 *
 * Available variables:
 * - attributes: HTML attributes for the select element.
 * - options: The available options.
 *
 * @see template_preprocess_select()
 */
#}

<select{{ attributes.addClass([
  'w-full',
  'px-3',
  'py-2',
  'border',
  'border-gray-300',
  'rounded-lg',
  'focus:outline-none',
  'focus:ring-2',
  'focus:ring-primary',
  'focus:border-transparent',
  'text-gray-900',
  'text-sm',
  'transition',
  'duration-200',
  'bg-white',
  'cursor-pointer'
]) }}>{{ options }}</select>