{#
/**
 * @file
 * Theme override for radio input elements.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_input()
 */
#}

<input{{ attributes.addClass([
  'h-4',
  'w-4',
  'text-primary',
  'bg-white',
  'border-gray-300',
  'focus:ring-2',
  'focus:ring-primary',
  'focus:ring-offset-0',
  'transition',
  'duration-200'
]) }} />{{ children }}