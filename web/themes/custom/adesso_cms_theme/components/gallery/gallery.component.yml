$schema: https://git.drupalcode.org/project/drupal/-/raw/11.x/core/modules/sdc/src/metadata.schema.json
name: Gallery
description: Editorial/Gallery component with media collection slot architecture
group: organisms
status: stable
props:
  type: object
  properties:
    pre_headline:
      type:
        - string
        - 'null'
      title: pre_headline
      description: The pre-headline of the gallery lightbox component
    section_title:
      type: string
      title: section_title
      description: The title of the gallery lightbox component
    sub_headline:
      type: string
      title: sub_headline
      description: The sub-headline of the gallery lightbox component
    modifier:
      type: string
      title: modifier
      description: The modifier class to apply to the gallery lightbox component
slots:
  gallery_items:
    title: Gallery Media Items
    description: Gallery media items collection (field_media_item)
    # removed: required: false (slots are optional by default)
