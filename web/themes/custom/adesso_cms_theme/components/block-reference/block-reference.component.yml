name: 'Block Reference'
props:
  type: object
  properties:
    pre_headline:
      type:
        - string
        - 'null'
      title: Pre-headline
      description: 'The pre-headline text'
    title:
      type: string
      title: Title
      description: 'The main headline'
    block_content:
      type: string
      title: Block Content
      description: 'The rendered block content'
    is_dark:
      type: [string, integer, boolean]
      title: Dark Theme
      description: 'Whether to use dark theme'

slots:
  content:
    title: 'Block Content'
    description: 'Referenced block content'
    # removed: required: true (slots are optional by default)
