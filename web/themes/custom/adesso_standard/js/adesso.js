import 'flowbite/dist/flowbite.js';

/**
 * @file
 * adesso_standard behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.adessoStandard = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
