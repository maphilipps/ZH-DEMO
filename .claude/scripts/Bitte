#!/bin/bash

# Wrapper für den deutschen Hook-Text
# "Bitte prüfe, ob du gegen keine Learnings aus dem .claude/llms.txt verstoßen hast, ansonsten nochmal drüber nachdenken."

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CLAUDE_DIR="$(dirname "$SCRIPT_DIR")"

# Führe Learnings-Validierung aus
"$CLAUDE_DIR/hooks/validate-learnings.sh" run

exit $?