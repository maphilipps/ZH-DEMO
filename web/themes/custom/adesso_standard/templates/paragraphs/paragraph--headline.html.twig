{#
/**
 * @file
 * Default theme implementation to display a paragraph.
 *
 * Available variables:
 * - paragraph: Full paragraph entity.
 * - content: All paragraph items.
 * - attributes: HTML attributes for the containing element.
 * - view_mode: View mode; for example, "preview" or "full".
 * - logged_in: Flag for authenticated user status.
 * - is_admin: Flag for admin user status.
 *
 * @see template_preprocess_paragraph()
 *
 * @ingroup themeable
 */
#}
{% include "@paragraph-headline/paragraph-headline.twig" with {
  attributes: attributes,
  paragraph: paragraph,
  content_width: content.field_content_width['#items'].0.value,
  preheadline: content.field_preheadline,
  headline: content.field_headline,
  heading_level: content.field_heading_level['#items'].0.value|default('h2'),
  subheadline: content.field_subheadline,
  view_mode: view_mode
} %}
